# Get the client

Already have the folder?

    SETUP.bat

That makes the Desktop shortcut, builds CodeStacks.exe if .NET csc exists, then launches the exe or PLAY.bat.

First machine:

    INSTALL.bat
    SETUP.bat

Update later:

    UPDATE.bat
