# One click

First time in the cloned folder:

    ONECLICK.bat

That writes a Desktop shortcut named CODE STACKS, builds CodeStacks.exe if .NET is present, then starts the client (exe → PLAY.bat).

After that: Desktop shortcut or ONECLICK.bat again.

Need files from GitHub first?

    INSTALL.bat
    ONECLICK.bat
